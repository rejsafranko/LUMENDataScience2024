CREATE TABLE IF NOT EXISTS reservations (
            reservation_id,
            night_number,
            stay_date,
            guest_id,
            guest_country_id,
            reservation_status,
            reservation_date,
            date_from,
            date_to,
            resort_id,
            cancel_date,
            room_cnt,
            children_cnt,
            total_price,
            room_category_id,
            sales_channel_id
        )